{
  "name": "codap-shared-table-plugin",
  "version": "0.1.0",
  "description": "CODAP plugin that implements collaborative table sharing",
  "homepage": ".",
  "license": "MIT",
  "repository": "https://github.com/concord-consortium/codap-shared-table-plugin",
  "dependencies": {
    "firebase": "^7.15.5",
    "iframe-phone": "^1.3.1",
    "p-debounce": "^2.1.0",
    "randomatic": "^3.1.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "^3.4.1"
  },
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "@types/node": "^12.12.47",
    "@types/react": "^16.9.41",
    "@types/react-dom": "^16.9.8",
    "npm-audit-resolver": "^2.2.0",
    "tslint": "^5.20.1",
    "tslint-react": "^4.1.0",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "audit": "check-audit",
    "audit:resolve": "resolve-audit",
    "lint": "tslint -c tslint.json -t verbose 'src/**/*.{ts,tsx}'",
    "lint:build": "tslint -c tslint-build.json -t verbose 'src/**/*.{ts,tsx}'",
    "lint:fix": "tslint -c tslint-build.json --fix 'src/**/*.{ts,tsx}'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:ci": "react-scripts test --no-watch",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
